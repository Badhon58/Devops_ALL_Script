Is it possible to attach a single volume to multiple Ec2 Instance = No.

### EFS (Elastic File System)

- EFS = EFS is only for ec2 linux instance
- FSX = Fsx is only for Windows EC2 Instance
- EFS = works with NFSv4 Protocol
- EFS is file based storage
- EFS is unlimited Storage
- EFS is unlimited Storage
- EFS doesn't require any pre-provisioning, (it will automatically increase and decrease based on the data you put in EFS)
- Efs can be mounted to multiple EC2 instances at the same tim eaccress az's

### Snow Family
