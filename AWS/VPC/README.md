## VPC

- AWS Virtual Private Cloud (VPC) is a virtual network that you create in AWS
- it allows you to lunch AWS resources, such as EC2 instances, into a virtual network that you define.
- This gives you control over your network environment, including IP address range selection,, subnets, route tables, and network gateways
